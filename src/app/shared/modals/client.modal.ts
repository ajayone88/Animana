export class client {
    public src: string;
    public name: string;
    public status: string;

    constructor(src: string, name: string, status: string ){
        this.src = src;
        this.name = name;
        this.status = status;
    };
}